version: '3.8'

services:
  jobboard:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount local data directory for persistence
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped
